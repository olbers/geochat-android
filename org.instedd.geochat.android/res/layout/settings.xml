<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
        android:title="@string/geochat_settings">
        
    <EditTextPreference
	    android:title="@string/geochat_number"
	    android:summary="@string/geochat_number_summary"
	    android:key="geochat_number"
	    android:id="@+id/geochat_number"/>
	    
	<ListPreference
		android:title="@string/refresh_rate"
		android:key="refreshRate"
		android:id="@+id/refreshRate"
		android:entries="@array/refresh_rate_entries"
		android:entryValues="@array/refresh_rate_entry_values"
		/>

</PreferenceScreen>